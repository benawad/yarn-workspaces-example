{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since",
    "new-version": "lerna version --conventional-commits --yes",
    "publish": "lerna publish",
    "publish-prerelease": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.10.6"
  },
  "name": "yarn-workspaces-example"
}
